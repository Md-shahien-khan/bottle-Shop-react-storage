// Step 1 made component Header and call in App.jsx
// Step 2 made bottles.json and get the json from programming
const Header = () => {
    return (
        <div>
            <h1>Vite + React</h1>
        </div>
    );
};

export default Header;